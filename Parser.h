#ifndef PARSER_H
#define PARSER_H

// Parser.h is kept for older files that include it directly.
// The parser implementation lives in FileOperations.h so CSV parsing stays consistent.
#include "FileOperations.h"

#endif
